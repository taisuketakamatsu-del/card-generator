// taisk configuration
//
// Fill these in with your own Supabase project's values
// (Project Settings > API in the Supabase dashboard), then re-deploy.
// The anon key is meant to be public/client-side — Supabase protects data
// through Row Level Security policies (see supabase-schema.sql), not by
// hiding this key.
//
// Until these are filled in, taisk runs in local-only demo mode: tasks are
// stored in this browser only, nobody else can see them, and there is no
// real password protection on the "private" tab.
window.TAISK_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",
};
